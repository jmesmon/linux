#include "clear.h"

#define __field(f_offset, f_size, f_name)
#define __count(c_offset, c_size, c_name)
#define __array(a_offset, a_size, a_name)
#define __bit_field(f_offset, f_size, f_name, f_bits)
#define REQUEST(r_value, index, r_fields)
